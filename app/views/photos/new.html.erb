<h1>Photos#new</h1>

<%= form_for(@photo, url: album_photos_path, :html_options => {:multipart => true}) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :url %>
  <%= f.file_field :url %>

  <%= f.submit "Add New Photo" %>

  <% end %>