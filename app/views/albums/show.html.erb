<h1>Albums#show</h1>

<ul>
  <%= @album.name %>
  <% @album.photos.each do |photo| %>
  <li>
    <%= link_to photo.name, album_photos_path(@album.id, photo.id) %>
    <%= image_tag photo.url %>
  </li>
  <% end %>
</ul>

<%= link_to 'Add a Photo to This Album', new_album_photos_path(@album.id) %>

